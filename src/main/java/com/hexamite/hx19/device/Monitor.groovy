package com.hexamite.hx19.device

import groovy.transform.Canonical

/**
 * Represents a monitor, HX19MS, that connects the positioning system with a PC. 
 **/
@Canonical
class Monitor extends RadioTransceiver {	
	// String toString() { "monitor(name: '$name')" }
}
